import { ButtonText } from "@/app/shared/button/button.text";
import { Component } from "@angular/core";

@Component({
  selector: 'app-header-hero',
  templateUrl: './header.hero.html',
  imports: [ButtonText]
})

export class HeaderHero {
}